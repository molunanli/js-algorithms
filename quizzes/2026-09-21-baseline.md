# 2026-09-21 基线摸底卷（作答版）

| 项 | 值 |
|----|----|
| 文件 | `quizzes/2026-09-21-baseline.md`（学习者作答版，**不含答案**） |
| 日期 | 2026-09-21 |
| 学习者 | LJS（四月） |
| 覆盖模块 | M01、M02、M03、M04、M05、M06、M08、M13（共 8 个模块） |
| 难度档 | 基线摸底（L0–L3 探测；本次不设 L4 专项题） |
| 题量 | 11 题 |
| 总分 | **100 分** |
| 作答方式 | **分 4 场独立计时**（跨日允许，须守「冷启动契约」） |
| 单场时限 | 场1 ≤15 min｜场2 ≤30 min｜场3 ≤20 min｜场4 ≤18 min（含 Q11 ≤26 min）；**全卷不设跨场累计时限** |
| 超时口径 | 每场独立计时，超时部分一律按空白计；不得挪用他场时限、不得回看／重做／重开续写 |

## 作答纪律（闭卷）

- **禁止**：网络检索、询问 AI 或他人、翻看任何题解／教案／历史作业。
- **允许**：运行自己写的代码、查看 `node` 报错信息、使用本地运行时自带的语言特性（不查外部资料）。
- 不会的题请**留空并标注「未接触／不会」**；留空是有效数据，不得靠搜索补齐。
- **每场独立计时**：单场超过该场建议总用时的部分，一律按空白计；不得挪用他场时限、不得回看／重做／重开续写（全卷不设跨场累计时限）。

## 题量一览表

| 题号 | 模块 | 题型 | 分值 |
|------|------|------|------|
| Q1 | M01 复杂度分析 | 概念选择（3 小题） | 9 |
| Q2 | M01 复杂度分析 | 复杂度分析（须写推导） | 11 |
| Q3 | M02 数组与字符串 | 手写代码（模板） | 10 |
| Q4 | M02 数组与字符串 | 手写代码（变式） | 12 |
| Q5 | M02 数组与字符串 | 找 bug + 边界用例设计 | 8 |
| Q6 | M03 双指针与滑动窗口 | 手写代码（2 子问） | 12 |
| Q7 | M04 哈希表 | 手写代码 + 说明（2 子问） | 12 |
| Q8 | M05 栈与队列 | 手写代码（2 子问） | 6 |
| Q9 | M06 链表 | 手写代码（2 子问） | 6 |
| Q10 | M08 排序与二分查找 | 手写代码（2 子问） | 6 |
| Q11 | M13 动态规划（条件释放） | 手写代码（2 子问） | 8 |
| **合计** | — | — | **100** |

## 分场切分与用时（依《需求书》§十 v1.1）

| 场 | 题号 | 模块 | 本场建议总用时 | 本场产出的判定 |
|----|------|------|--------------|---------------|
| 场1 闸门场 | Q1、Q2 | M01 复杂度分析 | **≤ 15 min** | M01 初始等级 |
| 场2 基础场 | Q3、Q4、Q5 | M02 数组与字符串 | **≤ 30 min** | M02 初始等级 + 起点模块敲定 |
| 场3 分层场 | Q6、Q7 | M03 双指针与滑动窗口、M04 哈希表 | **≤ 20 min** | M03 / M04 分层等级 |
| 场4 探测场（可选） | Q8、Q9、Q10；（Q11 条件释放） | M05、M06、M08；（M13） | **≤ 18 min**（含 Q11 ≤ 26 min） | 上限探测（效力封顶 L3） |

> **用时声明**：建议用时是**节奏提示，不是评分依据**；各场**独立计时**，超时按空白计。单题实际用时达建议用时 1.5 倍仍无可运行产物 → **留空并标注「不会」，转下一题**（仅为时间管理，不含任何解题提示）。

> **冷启动契约（跨场必守 7 条，依《需求书》§10.3）**：
> 1. **不得复习**：场间不得复习任何算法内容、不得回看教案／笔记／题解／资料；
> 2. **不得求助**：场间不得问 AI、他人，不得接受任何反馈或提示；
> 3. **不得回看自答**：场间不得回看本次基线卷**任何已作答内容**（含自己的答案与草稿）；
> 4. **不得重做**：不得重写、不得修改已提交的题；
> 5. **不得续写**：不得在二次进入同一场时限内续写前场未完成的题；
> 6. **独立计时**：每场开始时重新计时，各场时长互不挪用；
> 7. **分别落盘**：各场作答分别落盘、分别标注场次与日期，不合并不覆盖。

> **等价超时条款（依《需求书》§10.6(b)）**：**每场独立计时**。单场超过该场建议总用时的部分，**一律按空白计，不得事后补做、不得延长**；**不得挪用他场时限**补齐前场未完成的题；**不得回看、不得重做、不得重开同一场续写**。全卷**不设跨场累计时限**。

## 落盘与提交规范（作答前必读）

- **文字类答案**（Q1、Q2 推导、Q5 定位与边界清单、Q7b 说明、Q8b/Q9b/Q10b 边界清单、Q11b 说明）写入本文件对应题目的**作答区**。
- **手写代码**写入 `exercises/{模块编号-模块名}/{YYYY-MM-DD}-{slug}.js`（各题末给出确切路径）；模块目录不存在时按规范新建，**不得**写到 `quizzes/` 或其它位置。
- 每个练习文件头部必须写入五字段注释模板：`@module / @problem / @date / @complexity / @note`。
- 代码题请把用例写成 `console.assert(actual === expected, 'case#k: ...')` 并本地用 `node` 实跑；**无输出即全部断言通过**。
- 提交信息：答卷 `test(quiz): 2026-09-21 基线摸底`；练习 `feat(practice): 2026-09-21 {题目名}`。

---

> **【场1 闸门场】**（本场题目：Q1、Q2 ｜ 本场建议总用时 **≤ 15 min**）

## 一、概念选择

### Q1　【M01 复杂度分析 ｜ 概念选择 ｜ 9 分 ｜ 建议用时 ≤5 min】

以下 3 小题，每题只有一个正确选项。

**1-1（3 分）** 下列时间复杂度量级由小到大排列，正确的是：

- A. O(1) < O(n) < O(log n) < O(n log n)
- B. O(1) < O(log n) < O(n) < O(n log n)
- C. O(log n) < O(1) < O(n) < O(n²)
- D. O(n log n) < O(n) < O(n²) < O(2ⁿ)

**1-2（3 分）** 在 JavaScript 中，对一个含 n 个元素的数组执行一次 `arr.shift()`（从头部删除并返回首元素），其时间复杂度是：

- A. O(1)
- B. O(log n)
- C. O(n)
- D. 均摊 O(1)，与 `push` 同阶

**1-3（3 分）** 递归函数 `function f(n) { return n <= 1 ? 1 : f(n - 1) + 1; }`，在 n ≥ 1 时调用 `f(n)`，其**额外空间**复杂度主要由下列哪项决定：

- A. O(1)，没有任何额外空间
- B. O(n)，来自递归调用栈的深度
- C. O(log n)，因为每次规模折半
- D. O(n²)，来自重叠子问题的重复计算

> **【Q1 作答区】**
>
> 1-1 我的答案：__A____
> 1-2 我的答案：__C____
> 1-3 我的答案：__B____

---

## 二、复杂度分析（须写推导）

### Q2　【M01 复杂度分析 ｜ 复杂度分析（须写推导）｜ 11 分 ｜ 建议用时 ≤10 min】

阅读下面的 JavaScript 代码。**约定：`n` 表示二维数组 `rows` 中元素的总个数**（即 `rows` 所有行长度之和）。

```js
function flattenSum(rows) {
  let total = 0;
  for (let i = 0; i < rows.length; i++) {
    const row = rows[i];
    for (let j = 0; j < row.length; j++) {
      total += row[j];
    }
  }
  return total;
}
```

请写出 `flattenSum` 的**时间复杂度**与**空间复杂度**，并**逐步说明推导过程**。

> **【Q2 作答区】**
>
> 时间复杂度：_______O(n^2)_______
>
> 推导过程：
>
> （在此写出你的推导）
函数里面是两层循环，外层是O(n)，内层也是O(n)。所以整体时间复杂度是O(n^2)
>
> 空间复杂度：______O(1)________
>
> 推导过程：
>
> （在此写出你的推导）
因为只有一个变量total
---

> **【场2 基础场】**（本场题目：Q3、Q4、Q5 ｜ 本场建议总用时 **≤ 30 min**）

## 三、手写代码

> 以下所有代码题统一要求：使用标准 ES2020+ 语法、写出可被 `node` 直接运行的文件、在文件内用 `console.assert` 写用例。

### Q3　【M02 数组与字符串 ｜ 手写代码（模板）｜ 10 分 ｜ 建议用时 ≤8 min】

实现**前缀和**构建函数。

- 函数签名：`function prefixSums(nums)`
- 功能：返回一个新数组 `pre`，其中 `pre[i] = nums[0] + nums[1] + ... + nums[i]`；若 `nums` 为空数组，返回空数组。
- 约束：时间复杂度 O(n)，空间复杂度 O(n)；**不得修改入参 `nums`**。
- 示例：

```text
输入 nums = [1, 2, 3, 4]
期望输出 [1, 3, 6, 10]
```

> **【Q3 作答区】**
>
> 落盘路径：`exercises/M02-数组与字符串/2026-09-21-prefix-sums.js`
>
> 运行命令：`node "exercises/M02-数组与字符串/2026-09-21-prefix-sums.js"`
>
> 实跑输出（无输出＝全部断言通过）：

---

### Q4　【M02 数组与字符串 ｜ 手写代码（变式）｜ 12 分 ｜ 建议用时 ≤10 min】

实现 **n×n 矩阵的顺时针 90° 原地旋转**。

- 函数签名：`function rotate90(matrix)`
- 功能：把输入的 n×n 矩阵**原地**顺时针旋转 90°，并返回该矩阵（旋转后应与入参为同一个对象）。
- 约束：时间复杂度 O(n²)；**空间复杂度 O(1)**（不允许再新建一个 n×n 的矩阵）。
- 示例：

```text
输入 matrix = [[1, 2],
                [3, 4]]
期望输出 [[3, 1],
          [4, 2]]
```

> **【Q4 作答区】**
>
> 落盘路径：`exercises/M02-数组与字符串/2026-09-21-rotate-matrix.js`
>
> 运行命令：`node "exercises/M02-数组与字符串/2026-09-21-rotate-matrix.js"`
>
> 实跑输出（无输出＝全部断言通过）：

---

### Q5　【M02 数组与字符串 ｜ 找 bug + 边界用例设计 ｜ 8 分 ｜ 建议用时 ≤10 min】

待审代码文件：`quizzes/2026-09-21-bughunt-code.js`（**只读，请勿修改**）。

该文件中的 `removeElement(nums, val)` 按函数头部注释声明的需求实现，但存在 **1 处会导致可观察错误的实现缺陷**。请完成两件事：

**5a（4 分｜找 bug，须写全 3 项）**

1. 缺陷所在的**文件路径 : 行号**：______________
2. **错因分类**（从下列四类中择一）：______________
   - `概念不清`：对语言特性/数据结构语义理解有误（如引用与值、`slice` 与 `splice` 的区别）；
   - `边界遗漏`：未处理空/单元素/极端输入等边界；
   - `复杂度误判`：算法实际复杂度与需求或声明不符；
   - `实现 bug`：逻辑本身写错（下标越界、条件写反等）。
3. 该缺陷会导致的**可观察错误结果**（一句话）：______________

**5b（4 分｜边界用例设计）**

自拟**不少于 4 组**边界／反例用例。要求覆盖「**空数组 / 单元素 / 全相同 / 边界值（含负值 / 0 / 目标值不存在）**」中**至少 3 类**，并须包含**对「入参 `nums` 是否被修改」的检查**。

> **【Q5 作答区】**
>
> 5a 文件:行号：
> 5a 错因分类：
> 5a 可观察错误：
>
> 5b 边界用例表（至少 4 行）：
>
> | 序号 | 输入 nums | 输入 val | 期望返回值 | 备注（覆盖类别） |
> |------|-----------|----------|-----------|-----------------|
> | 1 |  |  |  |  |
> | 2 |  |  |  |  |
> | 3 |  |  |  |  |
> | 4 |  |  |  |  |
> | 5 |  |  |  |  |

---

> **【场3 分层场】**（本场题目：Q6、Q7 ｜ 本场建议总用时 **≤ 20 min**）

## 四、分层试探（双指针／哈希表）

### Q6　【M03 双指针与滑动窗口 ｜ 手写代码（2 子问）｜ 12 分 ｜ 建议用时 ≤10 min】

**6a（6 分）** 有序数组的两数之和（对撞指针）。

- 函数签名：`function twoSumSorted(nums, target)`
- 输入：`nums` 为**升序**整数数组，保证恰有唯一一组解 `i < j` 满足 `nums[i] + nums[j] === target`。
- 输出：返回下标数组 `[i, j]`（`i < j`）。
- 约束：时间复杂度 O(n)，空间复杂度 O(1)（**不得使用哈希表或额外存储**）。
- 示例：

```text
输入 nums = [2, 7, 11, 15], target = 9
期望输出 [0, 1]
```

> 作答落盘：`exercises/M03-双指针与滑动窗口/2026-09-21-two-sum-sorted.js`

**6b（6 分）** 无重复字符的最长子串长度（可变滑动窗口）。

- 函数签名：`function lengthOfLongestSubstring(s)`
- 输入：字符串 `s`（可为空串）。
- 输出：返回不含重复字符的最长连续子串的**长度**。
- 约束：时间复杂度 O(n)。
- 示例：

```text
输入 s = "abcabcbb"
期望输出 3    // 最长无重复子串为 "abc"
```

> 作答落盘：`exercises/M03-双指针与滑动窗口/2026-09-21-longest-substring.js`

> **【Q6 作答区】**
>
> 6a 实跑输出（无输出＝全部断言通过）：
>
> 6b 实跑输出（无输出＝全部断言通过）：

---

### Q7　【M04 哈希表 ｜ 手写代码 + 说明（2 子问）｜ 12 分 ｜ 建议用时 ≤10 min】

**7a（8 分）** 两数之和（哈希改造）。现有实现为双重循环枚举所有数对的 O(n²) 解法，请改写为 **O(n)** 解法。

- 函数签名：`function twoSum(nums, target)`
- 输入：整数数组 `nums`，保证恰有唯一一组解 `i < j` 满足 `nums[i] + nums[j] === target`。
- 输出：返回下标数组 `[i, j]`（`i < j`）。
- 约束：**时间复杂度 O(n)**；注意同一元素不能重复使用。
- 示例：

```text
输入 nums = [2, 7, 11, 15], target = 9
期望输出 [0, 1]
```

> 作答落盘：`exercises/M04-哈希表/2026-09-21-two-sum.js`

**7b（4 分）** 文字说明：

1. 指出 7a 中你的解法带来的**额外空间代价**：它从何而来、量级是多少；
2. 说明若把 `Map` 换成 `Object` 来存储**数字键**，可能引发什么**具体问题**。

> **【Q7 作答区】**
>
> 7a 实跑输出（无输出＝全部断言通过）：
>
> 7b 额外空间代价：
>
> （在此作答）
>
> 7b Object 键陷阱：
>
> （在此作答）

---

> **【场4 探测场（可选）】**（本场题目：Q8、Q9、Q10；Q11 条件释放 ｜ 本场建议总用时 **≤ 18 min**，含 Q11 则 **≤ 26 min**）

## 五、探测区（可放弃）

> 以下 Q8／Q9／Q10 为**上限探测题，可放弃**：做不完或不会写请直接留空，按空白计，**不影响**必答区判定。

### Q8　【M05 栈与队列 ｜ 手写代码（2 子问）｜ 6 分 ｜ 建议用时 ≤6 min】

**8a（4 分）** 有效的括号。

- 函数签名：`function isValid(s)`
- 输入：字符串 `s`，只包含 `(` `)` `[` `]` `{` `}` 六种字符。
- 输出：`true`（所有括号按正确顺序与种类闭合）或 `false`。
- 约束：时间复杂度 O(n)。
- 示例：

```text
输入 s = "()[]{}"   期望输出 true
输入 s = "(]"       期望输出 false
```

> 作答落盘：`exercises/M05-栈与队列/2026-09-21-valid-parentheses.js`

**8b（2 分）** 在代码文件中补充**不少于 3 组**边界用例（写成 `console.assert`），并在下方列出：`输入 → 期望输出`。

> **【Q8 作答区】**
>
> 8a 实跑输出（无输出＝全部断言通过）：
>
> 8b 边界用例（输入 → 期望输出）：
>
> 1.
> 2.
> 3.

---

### Q9　【M06 链表 ｜ 手写代码（2 子问）｜ 6 分 ｜ 建议用时 ≤6 min】

链表节点定义如下（可直接使用）：

```js
function ListNode(val, next) {
  this.val = (val === undefined ? 0 : val);
  this.next = (next === undefined ? null : next);
}
```

**9a（4 分）** 反转单链表（迭代法）。

- 函数签名：`function reverseList(head)`
- 输入：单链表头节点 `head`（可能为 `null`）。
- 输出：反转后的新头节点。
- 约束：迭代实现，不可改变节点内的值（只调整指针）；不得断链。
- 示例：

```text
输入 1 → 2 → 3 → null     期望输出 3 → 2 → 1 → null
```

> 作答落盘：`exercises/M06-链表/2026-09-21-reverse-list.js`

**9b（2 分）** 补充**不少于 3 组**边界用例（写成 `console.assert`），并在下方列出：`输入 → 期望输出`。

> **【Q9 作答区】**
>
> 9a 实跑输出（无输出＝全部断言通过）：
>
> 9b 边界用例（输入 → 期望输出）：
>
> 1.
> 2.
> 3.

---

### Q10　【M08 排序与二分查找 ｜ 手写代码（2 子问）｜ 6 分 ｜ 建议用时 ≤6 min】

**10a（4 分）** 搜索插入位置。

- 函数签名：`function searchInsert(nums, target)`
- 输入：`nums` 为**升序、无重复**的整数数组；`target` 为整数。
- 输出：若 `target` 存在，返回其下标；否则返回它应被插入以保持有序的下标。
- 约束：时间复杂度 **O(log n)**。
- 示例：

```text
输入 nums = [1, 3, 5, 6], target = 5   期望输出 2
输入 nums = [1, 3, 5, 6], target = 2   期望输出 1
```

> 作答落盘：`exercises/M08-排序与二分查找/2026-09-21-search-insert.js`

**10b（2 分）** 补充**不少于 3 组**边界用例（写成 `console.assert`），并在下方列出：`输入 → 期望输出`。

> **【Q10 作答区】**
>
> 10a 实跑输出（无输出＝全部断言通过）：
>
> 10b 边界用例（输入 → 期望输出）：
>
> 1.
> 2.
> 3.

---

## 六、触发区（条件释放）

### Q11　【M13 动态规划 ｜ 手写代码（2 子问）｜ 8 分 ｜ **条件释放** ｜ 建议用时 ≤8 min】

> **释放规则**：本模块为**条件释放**。只有当**探测区 Q8／Q9／Q10 中通过 ≥ 2 题**时，才发放本题作答；未达到该条件时**不答此题**（留空）。

**11a（6 分）** 爬楼梯。

- 函数签名：`function climbStairs(n)`
- 输入：整数 `n`（`n ≥ 1`），每次可以爬 1 级或 2 级台阶。
- 输出：爬到第 `n` 级台阶共有多少种不同的方法数。
- 约束：时间复杂度 O(n)，空间复杂度 O(1)。
- 示例：

```text
输入 n = 3   期望输出 3
输入 n = 5   期望输出 8
```

> 作答落盘：`exercises/M13-动态规划/2026-09-21-climb-stairs.js`

**11b（2 分）** 说明你在 `n = 1`、`n = 2` 时的初始化取值，并说明为什么本题可以做到 **空间 O(1)**。

> **【Q11 作答区】**
>
> 11a 实跑输出（无输出＝全部断言通过）：
>
> 11b 初始化与空间说明：
>
> （在此作答）

---

## 七、自述区（**不参与任何等级判定，仅用于方案编排**）

> ⚠️ 本节全部回答**不构成任何评分依据**。任何一条自述（包括「我是初学者」「我学过 X」「每天只能学 15 分钟」）都**不得**作为上调或下调任何模块等级的理由。填写与否不影响等级。

| 序号 | 字段 | 我的回答 |
|------|------|---------|
| 1 | 每日可投入学习时长（分钟） |  |
| 2 | 是否有算法基础；若有，学过哪些模块、用什么语言、何时学的 |  |
| 3 | 上次系统学习算法距今多久 |  |
| 4 | 学习目标：面试 / 进阶 / 兴趣 / 其它（可写目标岗位） |  |
| 5 | 偏好形式：先讲后练 / 直接上题 / 边讲边练 |  |
| 6 | 是否用过 LeetCode 等平台、大致做过多少题 |  |
| 7 | 每日固定的学习时段 |  |
